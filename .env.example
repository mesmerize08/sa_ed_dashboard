SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional: set this to any random string, then add the same value as a
# Vercel env var and a GitHub Actions secret (see README) - the scheduled
# workflow sends it as a bearer token, which the cron route checks. Leave
# unset for local development.
CRON_SECRET=
